<div align="center">

# Miku OS

**Экспериментальное ядро операционной системы на Rust**

*Работает на Rust и одном разработчике :D*

<img src="https://raw.githubusercontent.com/alunwrd/miku-os/main/docs/miku.png" width="220" alt="Miku Logo">

[![Language](https://img.shields.io/badge/language-Rust-orange.svg)](https://www.rust-lang.org/)
[![Architecture](https://img.shields.io/badge/arch-x86__64-blue.svg)]()
[![Status](https://img.shields.io/badge/status-experimental-yellow.svg)]()
[![License](https://img.shields.io/badge/license-MIT-lightgrey.svg)]()

</div>

---

> **Переводы:** [English](English_README.md) · [Japanese](Japanese_README.md) · [основной README](../README.md)
> **Описание ABI:** [MikuOS_ABI.md](MikuOS_ABI.md)
> **Заметки по GPU:** [TU116](Nvidia_tu116.md) · [GB206](Nvidia_gb206.md)

---

## О проекте

**Miku OS** это операционная система, написанная с нуля в среде `no_std` на Rust. Под ней нет ни libc,
ни какой-либо хостовой среды выполнения: раскладка памяти, обработка прерываний, планирование,
файловые системы и драйверы написаны здесь.

Что работает сегодня: вытесняющее SMP-ядро, пользовательские процессы в ring 3 с `fork`/`exec`/`wait`
и сигналами, динамический компоновщик с разделяемыми библиотеками, init-демон (mikuD), VFS с
ext2/ext3/ext4, блочный слой с write-back кэшированием, стек TCP/IP и пользовательский шелл.

> Весь код на Rust. Ассемблер встречается только в точке входа загрузчика, мосте syscall, трамплине
> для AP и переключении контекста, то есть там, где соглашение о вызовах должно быть точным.

**Масштаб:** ~71 500 строк ядра в 251 файле плюс ~30 700 строк пользовательских библиотек и программ.

---

## Состояние и честные ограничения

Это экспериментальная система. Ниже то, чего действительно нет или что сделано условно, чтобы таблицы
возможностей ниже не читались как обещание большего.

| Область | Состояние |
|:--|:--|
| **Вытеснение** | Новое. Проверено в QEMU на 1-16 CPU. Более ранняя naked-asm точка входа таймера вешала реальное железо, поэтому есть переключатель `PREEMPTIVE_TIMER` в `kernel/arch/x86_64/interrupts.rs`: поставь `false`, если физическая машина зависает после первого тика, и система откатится на кооперативное планирование |
| **Потоки в userspace** | Отсутствуют. Нет `clone`, нет `futex`. Много ядер использует только ядро ОС, пользовательский процесс однопоточный |
| **`poll` / `select`** | Отсутствуют. Программа не может ждать несколько дескрипторов сразу |
| **Терминал** | `termios` (канонический и raw режимы, эхо, сигнальные символы) и `ioctl` есть. Сессий, групп процессов, управления заданиями, pty и `/dev/tty` нет |
| **Шелл** | Разделён. `/bin/msh` работает в ring 3, но большинство команд остаётся во встроенном шелле, потому что они вызывают внутренности ядра, для которых нет syscall |
| **TLS** | Реализован внутри ядра. Разбор недоверенного сетевого ввода в ring 0 это не то место, где ему следует быть |
| **Тесты** | Модульных тестов нет. Корректность держится на smoke-тесте загрузки в QEMU в CI |
| **NVIDIA** | Bring-up в процессе: путь загрузки GSP-RM для TU116/TU117 и GB206. Пригодного драйвера дисплея пока нет |

---

## Ядро

| Компонент | Описание |
|:--|:--|
| **Архитектура** | x86_64, `#![no_std]`, `#![no_main]` |
| **Загрузка** | GRUB2 + Multiboot2, фреймбуфер с автоопределением BGR/RGB |
| **Адресное пространство** | Ядро в верхней половине по `0xFFFFFFFF80000000`, прямая карта HHDM по `0xFFFF800000000000` |
| **Защита** | GDT + TSS + IST (double fault, page fault, GPF), ring 0 / ring 3 |
| **Прерывания** | IDT: таймер, клавиатура, ATA IRQ 14/15, NMI, MCE, #UD, #NM, #PF, #GP, double fault, ошибка LAPIC, spurious, 3 вектора IPI, 16 векторов MSI |
| **Контроллер прерываний** | LAPIC + IO-APIC. Устаревший 8259 инициализируется и полностью маскируется |
| **Таймер** | Таймер LAPIC, 250 Гц, калибруется по каналу 2 PIT с проверкой диапазона и запасным значением |
| **SMP** | До 64 CPU (`MAX_CPUS`), перечисление через ACPI MADT, запуск INIT/SIPI, GDT/TSS/GS на каждый CPU |
| **Планирование** | vruntime в стиле CFS, очереди на каждый CPU, кража работы, вытесняющий таймер |
| **SSE** | CR0.EM=0, CR0.MP=1, CR4.OSFXSR=1, CR4.OSXMMEXCPT=1 |
| **Куча ядра** | 128 МиБ, статически в `.bss`, `linked_list_allocator` |
| **Стеки ядра** | 1 МиБ у BSP, 512 КиБ на поток, 64 КиБ на AP, все со сторожевой страницей |
| **Системные вызовы** | 72 (0-71) через MSR `SYSCALL`/`SYSRET`, naked-asm мост |
| **Часы реального времени** | CMOS/MC146818 читаются при загрузке, уточняются по NTP когда поднимается сеть |

### Последовательность загрузки

Каждый шаг печатается как `[boot] ok <имя>` в serial и на фреймбуфер:

```
Physical memory manager -> ACPI (RSDP/MADT) -> APIC -> IO-APIC -> LAPIC timer ->
Real-time clock -> IRQ routing -> Virtual file system -> Shared library cache ->
Block device probe -> Block device nodes (/dev) -> Network subsystem ->
Firmware store -> NVIDIA GPU probe -> Scheduler -> Firmware SMI silence ->
PS/2 keyboard -> Interrupts -> Timer calibration -> SMP (AP bringup) ->
mikuD init daemon
```

### Защита памяти самого ядра

Каждый механизм появился потому, что поймал реальную труднодиагностируемую ошибку:

- **Сторожевые страницы стеков** (`kernel/mm/kstack.rs`): каждый стек потока берёт `страниц + 1`
  смежных фреймов и разотображает нижний из HHDM. Переполнение вызывает page fault на самой
  инструкции, а обработчик называет владеющий pid. Образ ядра отображён одной 1-ГиБ huge-страницей,
  поэтому без этого переполнение молча затирает то, что аллокатор выдал следом.
- **Канарейка стека BSP** (`kernel/kcore/stack_guard.rs`): отравленная страница под загрузочным
  стеком, проверяется после каждого шага инициализации.
- **`IrqMutex`** (`kernel/kcore/irq_lock.rs`): выключает прерывания на время критической секции.
  Обязателен для любого замка, до которого дотягивается обработчик прерывания (`PMM`, `SWAP_MAP`,
  `EMERGENCY_POOL`, `REFCOUNTS`, `MSI_HANDLERS`), иначе обычный spin-lock вешает CPU против самого себя.
- **`SchedMutex`** (`kernel/kcore/sched_lock.rs`): коротко спинит, затем отдаёт CPU планировщику.
  Используется для замка VFS, который удерживается через дисковый ввод-вывод, где спин сжигает
  целые кванты времени.

---

## Управление памятью

### Физическая память (`kernel/mm/pmm.rs`)

Битовый аллокатор фреймов со сканированием по машинным словам, раздельные подсказки для одиночных и
смежных выделений, счётчики ссылок для copy-on-write и аварийный резервный пул для пути подкачки.

### Виртуальная память (`kernel/mm/vmm.rs`)

Четырёхуровневые таблицы за типом `AddressSpace`. Пользовательские адресные пространства копируют
записи PML4 256-511 из таблиц ядра, поэтому отображения ядра и HHDM общие и не расходятся.
Huge-страницы расщепляются по требованию, когда нужно отображение 4 КиБ (MMIO, сторожевые страницы).

### Подкачка (`kernel/mm/swap.rs`, `swap_map.rs`)

Обратное отображение физического фрейма в `(cr3, virt)`, вытеснение по алгоритму часов со старением и
закреплением, кодирование swap-PTE с сохранением исходных флагов. Освобождение памяти выполняет поток
**kswapd**: тик таймера только поднимает флаг, потому что вытеснение делает дисковый ввод-вывод и
берёт замки, которые нельзя трогать из обработчика прерывания.

### mmap (`kernel/mm/mmap.rs`)

Анонимные и файловые отображения, подкачка по требованию, copy-on-write при `fork`, `mprotect`, `msync`.

---

## Процессы и планирование

| Возможность | Детали |
|:--|:--|
| **Модель** | Один `Process` на поток, потоки ядра делят CR3 ядра, пользовательские процессы получают свой |
| **Создание** | `fork` (CoW), `exec`/`execve`, `wait4`, `kill`, сбор зомби |
| **Планировщик** | vruntime в стиле CFS с весами приоритетов, очереди на каждый CPU, выбор по минимальному vruntime |
| **Балансировка** | Размещение на наименее загруженном подходящем CPU плюс кража работы при опустевшей очереди |
| **Вытеснение** | По таймеру. Naked-заглушка строит кадр из 15 GPR и iret, который ждёт планировщик, и продолжает исполнение на возвращённом стеке |
| **Привязка к CPU** | 64-битная маска на процесс |
| **Сигналы** | Точка входа обработчика регистрируется из userspace, `sigreturn`, SIGINT/SIGQUIT/SIGTERM/SIGKILL/SIGCHLD |
| **Пул воркеров** | Размер по числу CPU из ACPI, ограничен диапазоном 4-32 |
| **Дескрипторы** | Таблица на процесс, клонируется при `fork`, освобождается при `exit`, а не при сборе зомби |

---

## Системные вызовы

72 записи, диспетчеризация из naked-моста `SYSCALL`, который через `gs` переключается на стек ядра
текущего CPU.

| Диапазон | Область |
|:--|:--|
| 0-10 | `exit`, `write`, `read`, `mmap`, `munmap`, `mprotect`, `brk`, `getpid`, `getcwd`, `set_tls`, `get_tls` |
| 11-17 | `open`, `close`, `seek`, `fsize`, `map_lib`, `sleep`, `uptime` |
| 18-27 | `stat`, `fstat`, `mkdir`, `rmdir`, `unlink`, `readdir`, `rename`, `link`, `chmod`, `chown` |
| 28-42 | `dup`, `dup2`, `truncate`, `write_file`, `symlink`, `readlink`, `pipe`, `chdir`, `statfs`, `fallocate`, `getxattr`, `setxattr`, `utimensat`, `fsync`, `punch_hole` |
| 43-47 | `fork`, `wait4`, `kill`, `exec`, `umask` |
| 48-55 | `getuid`, `getgid`, `geteuid`, `getegid`, `setuid`, `setgid`, `seteuid`, `setegid` |
| 56-66 | `socket`, `connect`, `send`, `recv`, `mmap_file`, `msync`, `bind`, `listen`, `accept`, `sendto`, `recvfrom` |
| 67-69 | `execve`, `sigentry`, `sigreturn` |
| 70-71 | `clock_gettime`, `ioctl` |

**Проверка пользовательских указателей** (`kernel/syscall/usercopy.rs`): диапазон обязан целиком лежать
в канонической пользовательской половине, каждая страница обходится с проверкой `PRESENT | USER` (и
`WRITABLE` где нужно), ленивые страницы VMA подгружаются заранее, а пути копируются в память ядра до
использования, чтобы их нельзя было переписать после проверки.

---

## Виртуальная файловая система

| Параметр | Значение |
|:--|:--|
| Вноды | 256 |
| Открытых файлов на процесс | 128 |
| Слоты монтирования | 8 (VFS) / 4 (драйвер ext) |
| Длина имени | 64 байта |
| Кэш страниц | 1024 страницы × 512 Б = 512 КиБ |
| Типы ФС | tmpfs, devfs, procfs, ext2, ext3, ext4, cowfs, pipefs |

Возможности: иерархическое пространство имён, жёсткие и символические ссылки, права с uid/gid/umask,
расширенные атрибуты, блокировки файлов, cwd на процесс, кэш dentry, хуки журнала, учёт квот,
вспомогательные средства адресуемого по содержимому хранения и узлы блочных устройств в `/dev`,
создаваемые из блочного слоя.

`/proc` отдаёт `uptime`, `meminfo`, `diskstats` и другое, `/dev` содержит консоль, null, zero, random и
блочные устройства, `/lib` держит десять предзагруженных разделяемых библиотек.

---

## Файловые системы

### ext2 / ext3 / ext4 (`kernel/fs/ext/`)

Драйвер определяет вариант на диске по битам возможностей суперблока и так же о нём и сообщает:
`ext2`, `ext3` или `ext4`, в логе монтирования, в `statfs()` и в `/proc`:

```
[miku_extfs] slot 0 drive 1 lba 0 - ext4 (journal=true extents=true 64bit=true
                                          metadata_csum=true flex_bg=true dir_index=true)
```

| Возможность | Детали |
|:--|:--|
| **Чтение** | Прямые, косвенные, дважды и трижды косвенные блоки, деревья extent из ext4, inline data |
| **Запись** | Выделение с подсказками по группам, truncate, punch hole, fallocate, rename, ссылки |
| **ext3** | Журнал в стиле JBD2: транзакции, дескрипторные и revoke-блоки, воспроизведение при монтировании |
| **ext4** | Деревья extent, 64-битные номера блоков, flex_bg, контрольные суммы метаданных (CRC32c), inline data |
| **Размер тома** | Ограничен файловой системой, а не драйвером: таблицы групп блоков растут на куче |
| **Целостность** | fsck, очистка потерянных inode при монтировании, TRIM/discard, `fiemap` |
| **mkfs** | Создание ext2/ext3/ext4 с режимом пробного прогона |

### Прочее

- **tmpfs** корневая файловая система, на кэше страниц
- **devfs**, **procfs** синтезируются
- **GPT** разбор таблицы разделов, узлы разделов, `partprobe`

---

## Стек хранения

```
VFS  ->  блочный слой  ->  буферный кэш  ->  драйвер  ->  устройство
```

- **Блочный слой** (`kernel/io/block/`): реестр устройств, отображение разделов, статистика ввода-вывода
  по устройствам, discard и write-zeroes, мониторинг здоровья (лог-страницы NVMe и ATA SMART),
  `/proc/diskstats`
- **Буферный кэш**: 512 чанков по 4 КиБ, 8-канальная ассоциативность, write-back с потоком-флашером
  **bdflush**
- **Очереди**: драйверы на `&self` с бесблокировочной диспетчеризацией, NVMe использует четыре очереди
  отправки на CPU

**Драйверы** (`kernel/drivers/block/`): `ata` (PIO + bus-master DMA), `ahci`, `nvme`, `virtio_blk`,
`ramdisk` (для образа прошивки, переданного GRUB).

---

## Сетевой стек

| Уровень | Поддержка |
|:--|:--|
| **Драйверы** | Intel e1000 (82540EM/82545EM/82574L/82579LM/I217), Realtek RTL8139/8168/8169, virtio-net |
| **Канальный** | Ethernet, ARP с кэшем |
| **Сетевой** | IPv4, ICMP (ping, traceroute) |
| **Транспортный** | UDP, TCP с полным конечным автоматом, повторные передачи, слушающие сокеты |
| **Прикладной** | Клиент DHCP, резолвер DNS, HTTP/1.1, HTTP/2, NTP |
| **Безопасность** | Клиент TLS 1.2/1.3: RSA, ECDHE, AES-GCM, длинная арифметика, сейчас в ring 0 |
| **Сокеты** | Таблица сокетов на процесс, `socket`/`bind`/`listen`/`accept`/`connect`/`send`/`recv`/`sendto`/`recvfrom` |

`netd` при загрузке выполняет DHCP и поднимает соединение автоматически.

---

## Ввод и консоль

- **Клавиатура PS/2**: инициализация контроллера, набор скан-кодов 1, бесблокировочное кольцо
- **USB**: контроллер xHCI, HID-клавиатура, передача управления от BIOS/EHCI
- **Консоль**: текстовая консоль на фреймбуфере с цветом, прокруткой и растровым шрифтом JetBrains Mono,
  генерируемым при сборке. Весь вывод консоли дублируется в serial, именно это делает осмысленными
  headless-загрузки и логи CI
- **Терминал** (`kernel/io/input/user_stdin.rs`): дисциплина линии с каноническим и raw режимами,
  управление эхом, Ctrl-C в SIGINT, Ctrl-\ в SIGQUIT, Ctrl-D как EOF, Ctrl-U стирает строку. Настройки
  доступны из ring 3 через `ioctl` (`TCGETS`, `TCSETS`, `TIOCGWINSZ`)

---

## mikuD, init-демон

PID 1. Отвечает за жизненный цикл сервисов, зависимости и перезапуски.

| Понятие | Значения |
|:--|:--|
| **Таргеты** | `SysInit`, `MultiUser`, `Graphical`, `Rescue` |
| **Политики перезапуска** | `Always`, `Never`, `OnFailure`, `OnSuccess`, `OnAbnormal` |
| **Точка входа сервиса** | Функция ядра `fn()` либо путь к ELF-файлу на диске |
| **Дополнительно** | Граф зависимостей, порядок запуска, watchdog, задержки перезапуска, маскирование, журнал, таймерные юниты, активация по сокету, unit-файлы `.service` |

Сервисы, регистрируемые при загрузке: `kbd`, `shell`, `netd`, `usbd`, `bdflush`, `kswapd`.

Сервис `shell` предпочитает `/bin/msh` (ring 3), если корневая ФС его содержит, иначе откатывается на
встроенный шелл. Выбор пишется в лог.

---

## Пользовательское пространство

### Динамическая компоновка

`ld-miku` это динамический загрузчик: разбор ELF64, отображение `PT_LOAD`, обработка перемещений
(`RELA`, `JMPREL`, `GLOB_DAT`, `JUMP_SLOT`, `RELATIVE`), разрешение символов между библиотеками, обход
зависимостей `DT_NEEDED`, настройка TLS и полный auxv.

Десять разделяемых библиотек предзагружаются в VFS по пути `/lib` и отдаются прямо из памяти образа
ядра, не затрагивая кэш страниц:

`core_miku`, `sys_miku`, `text_miku`, `ds_miku`, `algo_miku`, `codec_miku`, `fs_miku`, `net_miku`,
`parse_miku`, `libc_miku`

Страницы разделяются между процессами, отображающими одну библиотеку, приватными остаются только
сегменты с записью.

### `/bin/msh`, пользовательский шелл

Шелл в ring 3, построенный строго на syscall ABI, что и держит этот ABI честным.

**Встроенные команды:** `pwd`, `cd`, `ls`, `cat`, `stat`, `mkdir`, `rm`, `write`, `echo`, `wc`, `head`,
`grep`, `uptime`, `date`, `stty`, `pid`, `help`, `exit`

**Возможности шелла:** перенаправления `>` `>>` `<`, конвейеры `|` (по одному форкнутому процессу на
стадию) и стартовый скрипт `/etc/msh.rc`.

```
$ cat /p.txt | wc
3 3 14
$ ls /bin | grep msh
msh
```

### Сборка пользовательских программ

```bash
cd src/lib/userspace
cargo +nightly build --release --target x86_64-miku-app.json \
    -Z json-target-spec -Z build-std=core \
    -Z build-std-features=compiler-builtins-mem --bin msh
```

Builder делает это автоматически и раскладывает бинарники в `/bin` на корневом образе.

---

## Встроенный шелл

Пока больший из двух, потому что эти команды обращаются к внутренностям ядра, для которых ещё нет
syscall. Около 190 команд, в том числе:

- **Файлы**: `ls`, `cat`, `cd`, `cp`, `mv`, `rm`, `mkdir`, `tree`, `du`, `stat`, `ln`, `chmod`, `chattr`
- **ext, независимо от версии**: `extls`, `extcat`, `extwrite`, `extfsck`, `extinfo`, `extsync` и
  семейства `ext2*` / `ext3*` / `ext4*`
- **Монтирование**: `mount`, `umount`, `fs.list`, `fs.select`, `partprobe`, `gpt`, `mkfs.ext2/3/4`
- **Хранение**: `blkstat`, `blkdiscard`, `blkzero`, `smart`, `fstrim`, `fiemap`, `nvmestress`
- **Подкачка**: `mkswap`, `swapon`, `swapoff`, `swapinfo`
- **Процессы**: `ps`, `top`, `kill`, `nice`, `affinity`, `exec`
- **Сервисы**: `sv start|stop|restart|status|enable|disable|mask|journal|timer|analyze`
- **Сеть**: `net`, `ping`, `dhcp`, `ntp`, `wget`, `curl`, `fetch`, `traceroute`, `socket`
- **Компоновка**: `ldd`, `ldconfig`, `load`
- **GPU**: подкоманды `nvidia`
- **Система**: `info`, `heap`, `memmap`, `history`, `reboot`, `poweroff`

---

## Драйвер NVIDIA GPU

Работа по bring-up в процессе, драйвером дисплея это ещё не является.

| Семейство чипов | Карты | Состояние |
|:--|:--|:--|
| TU116 / TU117 (Turing) | GTX 1650 / 1660 | Запуск Falcon, FWSEC, ACR, SEC2, boot args GSP-RM, очереди сообщений |
| GB206 (Blackwell) | RTX 5060 / 5060 Ti | FSP, FMC, путь загрузчика GSP |

Раскладка модулей (`kernel/drivers/gpu/nvidia/`): `pci`, `mmio`, `chip`, `vbios`, `reset`, `msi`, `fb`,
`profile`, `generic`, общий `gsp_common/` (RPC, sysinfo) и `gtx1650/`, `rtx5060/` под конкретные чипы.

Прошивки не встроены в ядро. Они раскладываются в дерево `/lib/firmware`, упаковываются в образ ext2,
передаются ядру как модуль GRUB и монтируются по требованию, поэтому один ISO несёт и ядро, и прошивки
без второго диска.

**Блобы NVIDIA не хранятся в репозитории.** Это проприетарная подписанная микропрограмма вендора.
Сборка без них работает: образ прошивки просто не создаётся, а ядро сообщает `firmware unavailable`.
Как добавить их локально, описано в `kernel/drivers/gpu/nvidia/firmware/README.md`.

---

## Сборка и запуск

### Требования

| Инструмент | Для чего |
|:--|:--|
| Rust nightly + `rust-src` | `build-std` для bare-metal цели |
| `grub-mkrescue`, `xorriso`, `mtools` | Создание ISO |
| `qemu-system-x86_64` | Запуск |
| `e2fsprogs` (`mke2fs`, `debugfs`) | Корневой образ и раскладка прошивок |

### Собрать всё

```bash
cd builder
cargo run
```

Builder собирает `ld-miku`, библиотеки miku, пользовательские программы и ядро (release), создаёт ISO,
подготавливает `disk.img` с `/lib/firmware` и `/bin` и может запустить QEMU.

### Запуск вручную

```bash
qemu-system-x86_64 \
  -boot d -cdrom miku-os/miku-os.iso \
  -drive file=miku-os/disk.img,format=raw,if=none,id=disk0,cache=unsafe,aio=threads \
  -device ide-hd,drive=disk0,bus=ide.0,unit=1 \
  -serial stdio -display gtk -m 4G -smp 4 \
  -device qemu-xhci,id=xhci -device usb-kbd,bus=xhci.0
```

### Запись на флешку

```bash
sudo dd if=miku-os/miku-os.iso of=/dev/sdX bs=4M status=progress conv=fdatasync
```

---

## Структура репозитория

```
kernel/                 исходники ядра
  arch/x86_64/          точка входа, GDT, IDT, APIC, ACPI, SMP, RTC, per-CPU, serial
  mm/                   pmm, vmm, куча, mmap, swap, стеки ядра
  sched/                планировщик CFS, очереди, жизненный цикл, воркеры
  process/              Process, загрузчик ELF, динамическая компоновка, сигналы
  syscall/              диспетчер, адаптеры, usercopy, errno
  fs/                   VFS, ext2/3/4, tmpfs, devfs, procfs, mkfs, GPT
  io/                   блочный слой, консоль, фреймбуфер, ввод
  net/                  от Ethernet до TLS
  drivers/              block, bus (PCI), net, input (PS/2, USB), gpu (NVIDIA)
  mikud/                init-демон
  shell/                встроенный шелл
  kcore/                состояние загрузки, часы, прошивки, замки, питание, RNG, время
src/lib/                пользовательское пространство
  ld_miku/              динамический загрузчик
  libmiku/, mikulibs/   исходники стандартной библиотеки и библиотеки по доменам
  userspace/            программы ring 3 (msh, hello, тесты)
builder/                оркестрация сборки, подготовка ISO и дисков
docs/                   переводы, описание ABI, заметки по GPU
```

---

## Автор

**alunwrd** [github.com/alunwrd](https://github.com/alunwrd)

Написано на Rust, с нуля, одним человеком.

## Лицензия

MIT, см. [LICENSE](../LICENSE)
